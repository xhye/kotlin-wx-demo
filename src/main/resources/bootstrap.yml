spring:
  profiles:
    active: dev
  application:
    name: kotlin-wx-demo
  security:
    user:
      name: "user"
      password: "123456"
  cloud:
    bus:
      enabled: false
    config:
      enabled: false
    consul:
      enabled: false
logging:
  level:
    com.aegis.modules.*.dao: info
    com.aegis.modules.*.service: info